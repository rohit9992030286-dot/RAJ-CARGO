
'use client';

// This is a proxy component to reuse the hub shortages page.
// Admin users have full access via the useManifests hook.
import ShortagesPage from '@/app/hub/shortages/page';

export default ShortagesPage;
